{% extends 'base.html.twig' %}

{% block title %}Time Period Details{% endblock %}

{% block body %}
        <h1 class="mb-4">Time Period Details</h1>

        <table class="table table-striped table-bordered">
            <tbody>
            <tr>
                <th scope="row">ID</th>
                <td>{{ timePeriod.id }}</td>
            </tr>
            <tr>
                <th scope="row">Name</th>
                <td>{{ timePeriod.name }}</td>
            </tr>
            <tr>
                <th scope="row">Start Date</th>
                <td>{{ timePeriod.startDate|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th scope="row">End Date</th>
                <td>{{ timePeriod.endDate|date('Y-m-d') }}</td>
            </tr>
            <tr>
                <th scope="row">Working Days</th>
                <td>{{ timePeriod.workingDays }}</td>
            </tr>
            <tr>
                <th scope="row">Type</th>
                <td>{{ timePeriod.type.value }}</td>
            </tr>
            <tr>
                <th scope="row">Vélocité estimé</th>
                <td>
                    {% if timePeriod.estimatedVelocity %}
                        <strong>{{ timePeriod.estimatedVelocity|number_format(1) }} points/sprint</strong>
                        <button type="button" class="btn btn-sm btn-outline-info ms-2" data-bs-toggle="collapse" data-bs-target="#velocityExplanation">
                            <i class="fas fa-info-circle"></i> Comment c'est calculé ?
                        </button>
                    {% else %}
                        <em>Non calculée</em>
                    {% endif %}
                </td>
            </tr>
            <tr>
                <th scope="row">Vélocité communiqué</th>
                <td>{{ timePeriod.communicatedVelocity }}</td>
            </tr>
            </tbody>
        </table>

        {% if timePeriod.estimatedVelocity %}
            <div class="collapse mt-3" id="velocityExplanation">
                <div class="card card-body bg-light">
                    <h5 class="card-title">
                        <i class="fas fa-calculator"></i> Comment la vélocité estimée est calculée
                    </h5>
                    
                    <div class="row">
                        <div class="col-md-6">
                            <h6>📊 Formule de calcul :</h6>
                            <p class="mb-2">
                                <strong>Vélocité estimée = Vélocité globale × (Pourcentage de présence / 100)</strong>
                            </p>
                            
                            <h6>🔍 Détail du calcul :</h6>
                            <ol class="small">
                                <li>On calcule la capacité de chaque membre (jours travaillés × heures/jour)</li>
                                <li>On déduit les absences (congés, événements Google Calendar)</li>
                                <li>On calcule le pourcentage de présence = (jours présents totaux / jours théoriques totaux) × 100</li>
                                <li>On applique ce pourcentage à la vélocité globale configurée</li>
                            </ol>
                        </div>
                        
                        <div class="col-md-6">
                            <h6>💡 Exemple concret :</h6>
                            <ul class="small">
                                <li><strong>Vélocité globale :</strong> 39 points/sprint (100% de présence)</li>
                                <li><strong>Équipe de 5 personnes</strong> sur 10 jours = 50 jours théoriques</li>
                                <li><strong>3 personnes absentes 2 jours</strong> = 6 jours d'absence</li>
                                <li><strong>Jours présents :</strong> 44 jours</li>
                                <li><strong>Pourcentage de présence :</strong> (44/50) × 100 = 88%</li>
                                <li><strong>Vélocité estimée :</strong> 39 × 0.88 = <strong>34.3 points/sprint</strong></li>
                            </ul>
                        </div>
                    </div>
                    
                    {% if timePeriod.capacityData and timePeriod.capacityData.member_capacities %}
                        <div class="mt-3">
                            <h6>📈 Données actuelles de cette période :</h6>
                            <div class="row">
                                <div class="col-md-4">
                                    <strong>Vélocité globale :</strong> 39 points/sprint
                                    <br><small class="text-muted">
                                        <a href="{{ path('config_velocity') }}" target="_blank">
                                            <i class="fas fa-external-link-alt"></i> Voir l'explication
                                        </a>
                                    </small>
                                </div>
                                <div class="col-md-4">
                                    <strong>Membres de l'équipe :</strong> {{ timePeriod.capacityData.member_capacities|length }}
                                </div>
                                <div class="col-md-4">
                                    <strong>Durée :</strong> {{ timePeriod.workingDays }} jours ouvrés
                                </div>
                            </div>
                        </div>
                    {% endif %}
                </div>
            </div>
        {% endif %}

        <h2 class="mt-5">Capacity Data</h2>

        {% if timePeriod.capacityData is defined and timePeriod.capacityData is not empty %}
            <table class="table table-bordered mt-3">
                <thead class="table-light">
                <tr>
                    <th>Total Capacity</th>
                    <td>{{ timePeriod.capacityData.total_capacity }}</td>
                </tr>
                <tr>
                    <th colspan="2">Member Capacities</th>
                </tr>
                </thead>
                <tbody>
                {% for member, details in timePeriod.capacityData.member_capacities %}
                    <tr class="table-primary">
                        <th colspan="2">{{ member }}</th>
                    </tr>
                    <tr>
                        <td>Base Capacity</td>
                        <td>{{ details.base_capacity }}</td>
                    </tr>
                    <tr>
                        <td>Adjusted Capacity</td>
                        <td>{{ details.adjusted_capacity }}</td>
                    </tr>
                    <tr>
                        <td>Worked Days</td>
                        <td>{{ details.worked_days }}</td>
                    </tr>
                    <tr>
                        <td>Leave Days</td>
                        <td>{{ details.leave_days }}</td>
                    </tr>
                    <tr>
                        <td>Number of Events</td>
                        <td>
                            {% if details.events is not empty %}
                                {{ details.events|length }}
                            {% else %}
                                <em>No events</em>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        {% else %}
            <p class="text-muted">No capacity data available.</p>
        {% endif %}

        <div class="mt-4">
            <a href="{{ path('app_time_period_list') }}" class="btn btn-secondary">Back to list</a>
            <form action="{{ path('app_time_period_calculate_capacity', { 'id': timePeriod.id }) }}" method="post" class="d-inline">
                <button type="submit" class="btn btn-primary">Calculer la capacité</button>
            </form>
        </div>

        {% for message in app.flashes('success') %}
            <div class="alert alert-success mt-3">
                {{ message }}
            </div>
        {% endfor %}
{% endblock %}
